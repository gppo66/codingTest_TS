
import * as fs from 'fs';

const input : any[]= fs
  .readFileSync('dev/stdin')
  .toString()
  .split(' ')
  .map((value) => +(value));

    let answer : any = 0;
    
    
    console.log(answer);

    



